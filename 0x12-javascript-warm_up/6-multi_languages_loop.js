#!/usr/bin/node

// Print using loop

const lines = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
const count = lines.length;
let i = 0;

while (i < count) {
  console.log(lines[i]);
  i++;
}
